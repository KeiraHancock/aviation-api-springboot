server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db:5432/aviation
    username: aviation
    password: aviation
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
